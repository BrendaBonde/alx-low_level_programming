#include "main.h"

/**
 *  _abs - gives absolute value of an integer
 *  @i: input  number
 *
 *  Return: absolute value
 */
int _abs(int i)
{
	if (i >= 0)
	{
		return (1);
	}
	else
	{
		return (i * -1);
	}
}
