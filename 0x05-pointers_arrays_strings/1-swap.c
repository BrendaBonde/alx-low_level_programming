#include "main.h"

/**
 * swap_int - swaps integer values
 * @i: first value pointer
 * @j: second value pointer
 * Return: nothing
 */

void swap_int(int *a, int *b)
{
	*i = *i + *j;
	*j = *i + *j;
	*i = *i - *j;
}
